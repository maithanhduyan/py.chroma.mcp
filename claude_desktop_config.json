{
  "_comment": "Claude Desktop MCP configuration template for py-chroma-mcp",
  "_note": "Update the paths below to match your installation directory",
  "mcpServers": {
    "py-chroma-mcp": {
      "command": "python",
      "args": ["YOUR_PROJECT_PATH\\run_server.py"],
      "env": {
        "CHROMA_DB_PATH": "YOUR_PROJECT_PATH\\chroma_db"
      }
    }
  }
}
