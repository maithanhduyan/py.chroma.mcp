{
  "servers": {
    "chroma": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "src/server.py",
        "--client-type",
        "persistent",
        "--data-dir",
        "chroma_db"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}
